import { View, Text, StyleSheet} from "react-native";

export default function Home({route}) {
  const {username} = route.params
  return (
    <View >
      <Text> </Text>
      <Text></Text>
    </View>
  )};

const styles = StyleSheet.create({
        backgraudcolor: "#640C0C",
    });
